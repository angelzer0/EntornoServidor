package com.nominas;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Clase principal para la aplicación Spring Boot GestionNominasWebSpringApplication.
 * Esta clase contiene el método principal para iniciar la aplicación.
 */
@SpringBootApplication
public class GestionNominasWebSpringApplication {

    /**
     * Método principal para iniciar la aplicación Spring Boot.
     *
     * @param args Argumentos de línea de comandos pasados a la aplicación.
     */
    public static void main(String[] args) {
        SpringApplication.run(GestionNominasWebSpringApplication.class, args);
    }

}
